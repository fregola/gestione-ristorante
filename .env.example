# File di esempio per le variabili d'ambiente
# Copia questo file come .env e modifica i valori per la tua configurazione

# SECRET_KEY - IMPORTANTE: Genera una chiave casuale per produzione
# Usa: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=cambia-questa-chiave-con-una-casuale

# Configurazione Flask
FLASK_ENV=production
FLASK_DEBUG=False

# Database (se necessario)
# DATABASE_URL=sqlite:///production.db

# CORS - Domini consentiti per SocketIO (separati da virgola)
# Per sviluppo locale:
CORS_ORIGINS=http://localhost:5001,http://127.0.0.1:5001
# Per produzione VPS (esempio):
# CORS_ORIGINS=https://tuodominio.com,https://www.tuodominio.com

# SSL/HTTPS - Configurazione certificati SSL
# Per sviluppo locale: False (usa HTTP)
# Per produzione: True (usa HTTPS con cert.pem e key.pem)
SSL_ENABLED=False
# Nota: Per produzione, assicurati di avere cert.pem e key.pem nella directory root